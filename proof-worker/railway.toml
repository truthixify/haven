[build]
builder = "dockerfile"
dockerfilePath = "proof-worker/Dockerfile"
watchPatterns = ["proof-worker/**", "proof-worker/vendor/**"]

[deploy]
startCommand = "/app/proof-worker"
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
