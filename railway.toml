[build]
builder = "dockerfile"
dockerfilePath = "proof-worker/Dockerfile"
dockerfileContext = "proof-worker"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
