[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"
watchPatterns = ["proof-worker/**", "Dockerfile"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
